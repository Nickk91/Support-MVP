[build]
  command = "npm run build"
  publish = "dist"  # Change from "build" to "dist"

[build.environment]
  NODE_VERSION = "20"  # Upgrade to Node 20 for Vite compatibility

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200